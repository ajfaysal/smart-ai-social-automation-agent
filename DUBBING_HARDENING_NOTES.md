# Dubbing Production Hardening

This follow-up adds deployment configuration templates and a focused environment contract test for the facial landmark provider. The canonical setting is `MOUTH_LANDMARK_PROVIDER`; `LANDMARK_PROVIDER` remains a compatibility fallback. No provider is enabled by default.
